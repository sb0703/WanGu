import 'package:flutter/material.dart';

class Trait {
  final String id;
  final String name;
  final String description;
  final Color color;

  const Trait({
    required this.id,
    required this.name,
    required this.description,
    required this.color,
  });
}
